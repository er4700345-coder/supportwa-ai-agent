services:
  supportwa:
    build: .
    ports:
      - "4000:4000"
    env_file: .env
    volumes:
      - ./supportwa.db:/app/supportwa.db
  # For Postgres (uncomment if needed):
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: supportwa
  #     POSTGRES_USER: support
  #     POSTGRES_PASSWORD: support
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
# volumes:
#   pgdata: